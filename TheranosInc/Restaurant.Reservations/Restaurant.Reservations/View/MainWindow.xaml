<Window x:Class="Restaurant.Reservations.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:userControls="clr-namespace:Restaurant.Reservations.UserControls"
        Title="Reservation System"
        Width="423"
        Height="470">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Margin="5"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Text="Date:" />
        <DatePicker x:Name="ReservationDatePicker"
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,5,5,5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    DisplayDate="{Binding SelectedDate}"
                    DisplayDateEnd="{Binding EndDate}"
                    DisplayDateStart="{Binding StartDate}"
                    FirstDayOfWeek="Monday"
                    SelectedDate="{Binding SelectedDate,
                                           Mode=TwoWay}" />
        <TextBlock Grid.Row="0"
                   Grid.Column="2"
                   Margin="5"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Text="Time:" />
        <!--<DatePicker Grid.Row="0"
                    Grid.Column="3"
                    Margin="0,5,5,5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    DisplayDate="{Binding SelectedDate}"
                    DisplayDateEnd="{Binding EndDate}"
                    DisplayDateStart="{Binding StartDate}"
                    FirstDayOfWeek="Monday"
                    SelectedDate="{Binding SelectedDate}" />-->
        <userControls:ucDateTimeUpDown x:Name="TimePicker"
                                       Grid.Row="0"
                                       Grid.Column="3"
                                       Margin="0,5,5,5"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center" />
        <TextBlock Grid.Row="1"
                   Grid.Column="0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Text="Selected Date:" />
        <TextBlock Grid.Row="1"
                   Grid.Column="1"
                   Margin="5"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Text="{Binding SelectedDate,
                                  StringFormat='{}{0:dd-MMM-yyyy}'}" />

        <TextBlock Grid.Row="1"
                   Grid.Column="2"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Text="Selected Time:" />
        <TextBlock Grid.Row="1"
                   Grid.Column="3"
                   Margin="5"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Center"
                   Text="{Binding Path=SelectedTime,
                                  ElementName=TimePicker}" />

    </Grid>
</Window>